<Window x:Class="Google_Bookmarks_Manager_for_GPOs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Input" Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        TextElement.Foreground="{DynamicResource MaterialDesign.Brush.Foreground}"
        Background="{DynamicResource MaterialDesign.Brush.Background}"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="LabelTextBlock" Grid.Row="0" Text="Enter value:" Margin="0,0,0,10"
                   FontSize="14" FontWeight="Bold"/>

        <TextBox x:Name="InputTextBox" Grid.Row="1" Margin="0,0,0,20"
                 Style="{DynamicResource MaterialDesignTextBox}"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" Width="80" Margin="0,0,10,0"
                    Style="{DynamicResource MaterialDesignRaisedButton}"/>
            <Button Content="Cancel" Click="CancelButton_Click" Width="80"
                    Style="{DynamicResource MaterialDesignRaisedButton}"/>
        </StackPanel>
    </Grid>
</Window>
